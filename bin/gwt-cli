#!/usr/bin/env python3
import os
import sys

# Ensure local gwt_cli package is importable when running from git checkout
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from gwt_cli import main

if __name__ == "__main__":
    main()
